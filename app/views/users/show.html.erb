<div class="refresh-gist">
  <%= link_to "refresh", [ :get_gists, slug: @user.githubname ], method: :post %>
</div>
<% if @gists.present? %>
  <% @gists.each do |gist| %>
    <% unless gist.user != current_user && gist.hide %>
      <%= render Gist::CardComponent.new(gist) %>
    <% end %>
  <% end %>
<% end %>

