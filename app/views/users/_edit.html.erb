<%= simple_form_for user, url: user_path(slug: @user.githubname) do |f| %>
  <%= f.input :auto_import, wrapper: :custom_boolean_switch %>
  <%= f.input :only_group_import, wrapper: :custom_boolean_switch %>
  <%= f.input :stop_import, wrapper: :custom_boolean_switch %>
  <%= f.submit %>
<% end %>
